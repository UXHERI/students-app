version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip install -r requirements.txt
    build:
      commands:
        - echo "Building Flask app..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'